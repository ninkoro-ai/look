# Phase 6C Real Wardrobe Asset Validation

## 1. 实验目标

验证 LOOK 最核心的假设：**用户是否愿意把自己的真实衣服数字化，并持续使用**。本阶段不扩功能、不接会员/支付/社交，只验证“真实衣橱资产化流程”是否足够自然、可运行、可度量：

- 新用户第一次上传流程能否跑通（打开 → 添加衣服 → 衣橱出现第一件）；
- 两种上传方式（穿搭照片拆解 / 单品照片上传）是否可用；
- 衣橱资产能否被换装间正常读取；
- 过程中能否无感记录用户行为指标（不含隐私）。

## 2. 测试环境

| 项目 | 值 |
| --- | --- |
| 日期 | 2026-08-16 |
| 运行环境 | Windows 11（Asia/Shanghai），Edge headless（Playwright） |
| 构建 | Next.js 16.3.1 + TypeScript，`output: "export"`，`NEXT_PUBLIC_ENABLE_LAB=true` |
| 实验台 | `/lab/wardrobe`（仅实验环境，未影响 `/app` 正式流程） |
| AI 检测 | MockGarmentAIProvider（无 Key 自动回退 Mock，固定返回 5 件） |
| 数据存储 | IndexedDB v3（新增 `onboardingEvents` 分析存储，向后兼容） |
| 自动化回归 | qa.mjs 17/17 · qa-lab 13/13 · qa-wardrobe 11/11 · qa-import 10/10 · qa-model 10/10，全部 PASS |

> 重要说明：本阶段所有“用户行为”数据来自**自动化模拟会话**（Playwright 驱动实验台），用于验证流程与统计口径；真实用户意愿必须靠 Beta 实测。

## 3. 测试数据

建立 **Wardrobe Validation Dataset（20 组，全部为项目自绘演示素材，无真实用户照片）**：

| 场景 | 组数 | 样例 |
| --- | --: | --- |
| 女生自拍穿搭 | 5 | w01–w05 |
| 镜子自拍 | 5 | w06–w10 |
| 衣架照片 | 5 | w11–w15 |
| 平铺照片 | 5 | w16–w20 |

- 每个场景固定包含 5 件单品（牛仔外套 / 白色T恤 / 黑色阔腿裤 / 小白鞋 / 黑色单肩包），与 Mock 输出一致，用于流程与准确率统计口径验证；
- 样例由自绘 SVG 场景经无头渲染栅格化为 PNG（`public/lab-samples/wardrobe/w01–w20.png`）；
- 数据集定义：`lib/ai/wardrobe/dataset.ts`；样例生成脚本：`scripts/gen-wardrobe-samples.mjs`；
- 合规：未使用、未上传任何真实用户照片或未授权素材。

## 4. 用户流程

```
打开 LOOK
  ↓
实验入口「添加我的第一件衣服」（/lab/wardrobe）
  ↓
上传方式：○ 穿搭照片（拆解多件） ／ ○ 单品照片（单件）
  ↓
AI 识别（Mock 固定 5 件；真实 Provider 可替换）
  ↓
人工确认：改名 / 改分类 / 删除或恢复
  ↓
确认后生成透明素材（extractGarment）
  ↓
加入衣橱（IndexedDB wardrobe）
  ↓
换装间 / 衣橱正常读取
```

全程记录 `onboardingEvents`（上传开始/识别完成/开始导入/导入完成/放弃），**不记录照片、缩略图、姓名、手机号等隐私字段**（QA 已断言事件 JSON 无 `data:image` 等隐私数据）。

## 5. 穿搭照片测试结果

模拟会话（outfit 模式，样例 w01）：

| 指标 | 结果 |
| --- | ---: |
| 检测到单品 | 5 |
| 用户确认 | 4（删除 1 件 T 恤） |
| 用户修改 | 1（牛仔外套改名） |
| 加入衣橱 | 4 件 |
| 识别耗时（Mock） | 2ms |
| 整单处理耗时（上传→入库，Mock） | 137ms |
| 衣橱可见 | ✅ 39 → 衣橱卡片 +4 |
| 换装间读取 | ✅ 与现有换装链路共用 WardrobeItem，直接可用 |

## 6. 单品照片测试结果

模拟会话（single 模式，样例 w11 衣架照片）：

| 指标 | 结果 |
| --- | ---: |
| 检测到单品 | 5 |
| 用户选择 | 1 |
| 加入衣橱 | 1 件 |
| 整单处理耗时（Mock） | 74ms |
| 衣架/平铺 10 组场景识别 | ✅ 全部成功（w11–w20） |

## 7. AI 识别准确率

| 口径 | 结果 |
| --- | ---: |
| 类别准确率（20 组样例 vs 期望） | 100%（Mock 演示数据，构造一致） |
| 颜色准确率（20 组样例 vs 期望） | 100%（同上） |
| 真实照片准确率 | 未测（需 RealGarmentAIProvider + API Key） |

> 准确率统计口径已内置实验台（`lib/ai/wardrobe/analytics.ts`）：对带 `caseId` 的识别事件，将检出类别/颜色与数据集期望逐项比对。**Mock 场景下准确率 100% 属预期**（演示图按 Mock 输出绘制）；真实准确率必须用真实用户照片 + Real Provider 验证，这是 Beta 的前置条件。

## 8. 平均处理时间

| 阶段 | Mock（实测） | 真实 AI（预估） |
| --- | ---: | ---: |
| 上传→AI 识别 | 2ms | 1–5s（视网络/模型） |
| 透明素材提取 | <100ms | 1–3s |
| 整单（上传→入库） | 106ms | 2–8s |

目标 <30 秒：**模拟流程远超目标**；即使真实 AI，预估仍远低于 30s，满足“单件衣服处理时间”目标。

## 9. 用户修改成本

模拟会话统计（2 次完整导入）：

| 指标 | 平均 |
| --- | ---: |
| 修改次数（改名/改类） | 0.5 次/单 |
| 删除件数 | 0.5 件/单 |
| 放弃导入 | 0 次 |

UI 支持：名称编辑、分类下拉、删除/恢复、逐件确认；人工确认流程必须保留（类别识别必然存在错误）。

## 10. 失败案例

| 案例 | 原因 | 处理 |
| --- | --- | --- |
| headless Edge 对 SVG 样例执行 `createImageBitmap` 挂起 | 浏览器兼容问题（SVG 非真实用户输入格式） | 样例改投 PNG，规避；真实上传只收 JPG/PNG/HEIC |
| 无真实识别失败案例 | Mock 固定输出 5 件 | 真实误检/漏检需真实照片验证，已列入 Beta 前置条件 |

## 11. 产品问题（Top 5）

1. **Mock 无法体现真实识别错误**：固定返回 5 件且永远“正确”，无法度量真实误检/漏检对用户耐心的损耗；
2. **演示素材≠真实平铺照**：透明素材质量（边缘、阴影）需真实服装照片复测；
3. **失败引导弱**：识别失败时仅提示换图，没有“重拍/从相册重选/手动录入”兜底；
4. **正式产品缺少首启引导**：实验入口在 lab，正式首页/衣橱没有“添加我的第一件衣服”CTA；
5. **无真实用户样本**：本阶段为自动化模拟，“用户是否愿意”尚未被回答。

## 12. 优化建议

- Beta 前接入真实识别（用户授权照片 + RealGarmentAIProvider 或人工标注兜底），并做 20 组真实照片准确率基线；
- 正式首启加“添加我的第一件衣服”CTA，默认全选识别结果、只删错误项，降低确认成本；
- 失败路径补充“重拍 / 从相册重选 / 手动录入单件”；
- 沿用本实验台的 `onboardingEvents` schema 记录真实用户行为（不含隐私），继续用同一套统计口径；
- 用真实平铺照替换演示素材复测提取质量与耗时。

## 13. 是否适合进入 Beta 测试

**结论：流程与数据链路已可运行，建议进入小范围 Beta（10–20 人内测）验证真实意愿；不建议直接公测。**

理由：
- ✅ 首次建立衣橱流程、两种上传方式、资产入库、换装间读取全部跑通，指标采集完整；
- ⚠️ 但“用户是否愿意持续使用”只能由真人回答——自动化模拟给不出答案；
- ⚠️ 真实 AI 准确率尚未验证，Beta 需真实照片 + 真实 Provider（或人工兜底），否则会放大 Mock 与现实的差距。

---

## 附：验收状态

| 验收项 | 状态 |
| --- | --- |
| 新用户第一次上传流程可运行 | ✅ 模拟全流程跑通 |
| 穿搭照片拆解流程可运行 | ✅ w01 检出5→确认4→入库4 |
| 单品照片上传流程可运行 | ✅ w11 检出5→选1→入库1 |
| 衣物资产正常保存 | ✅ IndexedDB wardrobe |
| 衣橱正常展示 | ✅ qa-import 验证 |
| 换装间正常读取 | ✅ 共用 WardrobeItem 链路 |
| Demo 数据不受影响 | ✅ qa.mjs 35 件演示未变 |
| 无隐私数据记录 | ✅ 事件无照片/姓名/手机号（QA 断言） |
| 测试数据集完成 | ✅ 20 组（4 场景 × 5） |
| 数据统计完成 | ✅ 指标表见上 + `benchmarks/wardrobe-onboarding-20260816.json` |
| PHASE_6C_REPORT.md 完成 | ✅ 本文件 |
| Git 提交 | ✅ `feat: phase 6c real wardrobe validation` |
