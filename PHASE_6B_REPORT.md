# Phase 6B — 国内 VTON Provider Benchmark

## 1. 实验目标

在**不破坏现有产品**的前提下，建立国内云端 VTON Provider 的真实效果、速度、成本基准，并与 Phase 6A 已有的两个本地方案做统一 Benchmark；重点接入**阿里云百炼 AI试衣（aitryon / OutfitAnyone API）**，验证“本地即时换装 + 云端 AI 真实试穿按次计费”是否可以作为 LOOK 的低成本商业化架构。

本阶段是技术选型实验，不扩展产品功能、不接入首页/衣橱/换装正式流程、不默认开启生产云端调用。

---

## 2. 测试环境

| 项目 | 值 |
| --- | --- |
| 日期 | 2026-08-16 |
| 运行环境 | Windows 11（Asia/Shanghai），Edge headless（Playwright） |
| 构建 | Next.js 16.3.1 + TypeScript，`output: "export"`，`NEXT_PUBLIC_ENABLE_LAB=true` |
| 实验台 | `/lab/vton`（生产仅实验室可见） |
| 服务端代理 | Cloudflare Pages Functions `functions/api/vton/*` |
| 人物样例 | 本地 3 张全身照（`public/lab-samples/person-1/2/3.jpg`，约 0.8MB，正面/轻微侧身/长发） |
| 衣物素材 | 项目自绘演示 SVG 单品（透明背景），非真实服装照片 |
| 检测运行时 | MediaPipe Pose + Selfie Segmentation（浏览器本地） |

> 重要限制：演示衣物为**矢量插画而非真实服装平铺照**，对云端模型的还原度评估是“下界”参考；真实测试建议使用真实平铺服装图。

---

## 3. Benchmark Dataset

固定 20 组输入（所有 Provider 使用完全相同的 personImage + garmentImage + category）：

| 分组 | 组数 | 覆盖 |
| --- | --- | --- |
| A. T恤 | 5 | 纯白短袖 / 浅粉短袖 / 卡其工装短袖（组合 3 个人物样例） |
| B. 衬衫 | 5 | 米白真丝衬衫 / 白色雪纺衬衫 / 藏蓝条纹长袖 |
| C. 外套 | 5 | 黑色西装外套 / 卡其风衣 / 燕麦色开衫 / 米白羽绒服 / 军绿夹克 |
| D. 连衣裙 | 5 | 黑色吊带长裙 / 红色 A 字裙 / 奶油针织连衣裙 |

- Case ID：`b01`–`b20`（沿用 Phase 6A 的 `b01`–`b10` ID，并补充 `b11`–`b20`）
- 数据集定义：`lib/ai/vton/benchmark.ts`
- 合规：全部为本地演示素材，未使用、未上传任何真实用户照片

---

## 4. Provider 列表

| Provider | ID | 类型 | 状态 |
| --- | --- | --- | --- |
| Local Layer | `local-layer` | 本地图层拼接（姿势锚点） | ✅ 已实测 20/20 |
| Local Segmentation | `hybrid-mask` | MediaPipe 人像分割蒙版合成 | ✅ 已实测 20/20 |
| Alibaba VTON | `alibaba-vton` | 阿里云百炼 aitryon（服务端代理） | ⏸ 已接入，待真实 Key 实测 |
| OpenAI Image Edit | `openai-image-edit` | gpt-image-1 图像编辑 | ⏸ 已接入，无服务端 Key 跳过（按任务书不等待） |

统一契约见 `lib/ai/vton/contract.ts`；Phase 6A 三个旧 Provider 通过 `adaptLegacyProvider` 包装，**核心逻辑未重写**。

---

## 5. 成功率

| Provider | 测试数 | 成功 | 失败 | 成功率 |
| --- | --: | --: | --: | --: |
| local-layer | 20 | 20 | 0 | 100% |
| hybrid-mask | 20 | 20 | 0 | 100% |
| alibaba-vton | — | — | — | 未配置（AUTH_ERROR 路径已验证） |
| openai-image-edit | — | — | — | 未配置（跳过） |

完整逐条记录见 `benchmarks/vton-benchmark-20260816.json`（净化版，不含图片字节）。

---

## 6. 延迟

| Provider | 平均 | P50 | P95 | 最快 | 最慢 |
| --- | --: | --: | --: | --: | --: |
| local-layer | 404ms | 325ms | 355ms | 314ms | 1842ms |
| hybrid-mask | 1306ms | 1212ms | 1343ms | 1174ms | 3030ms |
| alibaba-vton | 预计 15–30s（官方文档口径） | — | — | — | — |

> 本次 local-layer 比 Phase 6A 快，原因为本机 MediaPipe 模型已缓存，仅首次加载有冷启动成本。

---

## 7. 成本

按官方计费：aitryon **0.20 元/张**，仅成功计费；新账号免费额度 **400 张 / 90 天**；汇率按 1 USD ≈ 7.2 CNY。

| Provider | 单次成本（USD） | 20 组总成本（USD） |
| --- | ---: | ---: |
| local-layer | $0 | $0 |
| hybrid-mask | $0 | $0 |
| alibaba-vton | ≈ $0.028（0.2 CNY） | ≈ $0.56（4 CNY） |
| openai-image-edit | ≈ $0.06（估算，未实测） | — |

---

## 8. 视觉质量评分

实验台已支持 6 维人工评分（1~5 分）并按权重合成：

`人脸保持 20% + 人体保持 15% + 衣服还原 25% + 边缘自然度 10% + 遮挡关系 10% + 颜色/纹理 10% + 速度 5% + 成本 5%`

以下是针对本次演示素材的**初始人工参考分**（可在实验台逐条复评）：

| Provider | 人脸 | 人体 | 衣服还原 | 边缘 | 遮挡 | 纹理 | 综合分 |
| --- | --: | --: | --: | --: | --: | --: | --: |
| local-layer | 5 | 4 | 2 | 2 | 2 | 2 | 3.20 |
| hybrid-mask | 5 | 4 | 3 | 3 | 3 | 3 | 3.70 |

说明：本地方案完全保留原图人脸/身体（不改像素），因此人脸、人体保持高分；但只做“贴图”而非真实光影融合，衣服还原、边缘、遮挡、纹理为短板。hybrid-mask 因先擦除原衣再叠加，整体自然度优于 local-layer。

---

## 9. 失败案例

本阶段本地实测 40/40 成功，无失败案例。

阿里云路径（无 Key 时）已通过服务端代理验证的预期失败：

| 场景 | 错误码 | 说明 |
| --- | --- | --- |
| 服务端未配置 `DASHSCOPE_API_KEY` | `AUTH_ERROR` | 代理返回 503，浏览器不接触任何 Key |
| 服务端未开 `VTON_ALLOW_ALIBABA=true` | `PROVIDER_ERROR` | 安全默认关闭 |
| 图片 <5KB / >5MB / 非支持格式 | `IMAGE_ERROR` | 服务端二次校验 |
| 任务超时（>180s） | `TIMEOUT` | 客户端轮询上限 |
| 阿里云限流（并发 5 / RPS 10） | `RATE_LIMIT` | 官方错误码映射 |

---

## 10. 各 Provider 优缺点

### Local Layer（本地图层拼接）
- ✅ 极快（P50 325ms）、$0、隐私 100% 本地、离线可用
- ❌ 无光影融合、边缘生硬、版型贴合差、只适合“快速预览换装”

### Local Segmentation（MediaPipe 分割蒙版）
- ✅ 快（P50 1212ms）、$0、隐私本地、先擦除原衣更自然
- ❌ 仍是无生成式融合，复杂姿势/遮挡效果有限，依赖 MediaPipe 运行时（约 18MB）

### Alibaba aitryon（云端 AI 试衣）
- ✅ 生成式试穿、服装纹理/版型还原强、国内节点无需翻墙、0.2 元/张、失败不收费、按量付费
- ❌ 15–30s 延迟、并发 5、图片规范严（5KB–5MB、150–4096px、公网 URL）、需要服务端代理与 Key
- ⏸ 本阶段因无 Key 未实测效果；接 Key 后可直接在 `/lab/vton` 跑 20 组

### OpenAI gpt-image-1
- ✅ 效果好（业界公认）、有免费/低配可用路径
- ❌ 跨境网络、无国内支付/账号、$0.06/张 单价更高、需要服务端 Key（未提供，跳过）

---

## 11. 免费换装方案评价

**建议保留 Local Layer + Local Segmentation 作为免费换装。**

理由：
1. 成本 $0，支撑高频“日常搭配预览”不会产生任何 API 消耗；
2. 隐私友好（照片不出浏览器）；
3. 速度 <1.5s，体验即时；
4. 它的短板（真实感）正好由低频、高价值的“AI 真实试穿”云端服务补足。

---

## 12. 国内云 VTON 评价

### 中国访问
- Endpoint：`dashscope.aliyuncs.com`（华北2·北京），中国大陆直连；
- 本机实测（Asia/Shanghai）：TCP 连接 ≈ 0.03s，HTTP 总耗时 ≈ 0.22s（未带 Key 返回 401，即网络可达）；
- 结果图存储：`oss-cn-hangzhou` 国内节点；
- 支持国内账号/支付宝支付；无需跨境网络。

### 商业许可
- 官方 API（aitryon）：商用按量付费，无额外模型授权，**适合商业生产**；
- ⚠️ OutfitAnyone **开源权重为研究用途**（非商业许可），**自部署权重 = NOT SUITABLE FOR COMMERCIAL PRODUCTION**；商业产品必须走官方 API；
- MediaPipe（Apache-2.0）：商用允许。

### 风险
- 限流：同时处理任务 5、RPS 10，峰值场景需排队；
- 免费额度仅 400 张/90 天，正式上线前需评估余额与充值；
- 图片需公网 URL：生产建议 OSS 而非临时上传通道（官方明确临时通道不适合生产）。

---

## 13. 成本模型

假设：90% 免费用户 + 10% 会员；免费用户平均 0.5 次 VTON/天（0~1 区间中值）；会员平均 25 次/月（20~30 区间中值）；汇率 7.2。

### 策略 A：所有换装都调用云端 VTON（0.2 CNY/张）

| 规模 | 免费用户月调用 | 会员月调用 | 月总调用 | 月成本 CNY | 月成本 USD |
| --- | --: | --: | --: | --: | --: |
| 100 用户 | 1,350 | 250 | 1,600 | ¥320 | ≈ $44 |
| 1,000 用户 | 13,500 | 2,500 | 16,000 | ¥3,200 | ≈ $444 |
| 10,000 用户 | 135,000 | 25,000 | 160,000 | ¥32,000 | ≈ $4,444 |

单用户月均：免费用户 ¥3.0，会员 ¥5.0。

### 策略 B：普通换装本地免费 + 「AI真实试穿」云端按次（免费用户 1.5 次/月，会员 10 次/月）

| 规模 | 免费用户月云端调用 | 会员月云端调用 | 月总调用 | 月成本 CNY | 月成本 USD |
| --- | --: | --: | --: | --: | --: |
| 100 用户 | 135 | 100 | 235 | ¥47 | ≈ $6.5 |
| 1,000 用户 | 1,350 | 1,000 | 2,350 | ¥470 | ≈ $65 |
| 10,000 用户 | 13,500 | 10,000 | 23,500 | ¥4,700 | ≈ $653 |

单用户月均：免费用户 ¥0.3，会员 ¥2.0。

### 结论

- 策略 B 相比策略 A 成本下降约 **85%**（10,000 用户规模下月成本从 ¥32,000 → ¥4,700）；
- 免费额度（400 张/90 天）可覆盖 100 用户规模下策略 B 的早期用量；
- “本地即时换装 + 云端高级 AI 试穿”可以作为 LOOK 低成本商业化架构。

---

## 14. 推荐方案

### Primary VTON Provider（云端候选）

```text
Alibaba VTON（aitryon）
```

理由：国内访问最优、单价最低（0.2 CNY/张）、失败不收费、商用许可清晰；待真实 Key 实测后确认最终视觉效果。

### 免费换装

```text
Local Layer + Local Segmentation（保留，$0）
```

### 产品形态

```text
普通换装（高频）→ 本地免费
AI 真实试穿（低频高价值）→ 云端 aitryon 按次计费
```

---

## 15. 后续建议

1. 提供 `DASHSCOPE_API_KEY`（北京地域）后，在 Cloudflare Pages 设置 Secret 并置 `VTON_ALLOW_ALIBABA=true`，即可在 `/lab/vton` 一键跑完 20 组真实云端 Benchmark，补齐效果/延迟/成本真实数据；
2. 生产环境不要使用临时上传通道（48h 有效、100QPS 限制），改用阿里云 OSS 公网 URL；
3. 云端 Provider 只开放给“会员/AI试穿次数”场景，并做服务端配额与并发控制（并发 5）；
4. 用真实服装平铺照替换演示 SVG 素材复测，验证衣服还原分；
5. 免费额度 400 张/90 天可作为冷启动期成本缓冲，正式运营前确认账户余额与账单预警；
6. 若追求更高清晰度/logo 还原，可对比 `aitryon-plus`（0.5 CNY/张）；
7. 不建议自部署 OutfitAnyone 权重（研究用途许可，不适合商业）。

---

## 16. 本阶段明确不做的事情

- ❌ 未接入首页/衣橱/换装正式流程（云端 VTON 仅存在于 `/lab/vton`）
- ❌ 未默认开启生产云端调用（默认 `VTON_PROVIDER=mock`，且云端需 Key + `VTON_ALLOW_ALIBABA=true` 双条件才可用）
- ❌ 未增加会员/支付/社交/分享/聊天/积分/广告
- ❌ 未删除或重写任何 Phase 6A Provider / Mock Provider
- ❌ 未收集、未上传任何真实用户隐私照片
- ❌ 未将任何 API Key 写入前端、Git、JSON、报告
- ❌ 未进入 Phase 7

---

## 附：验收状态

| 验收项 | 状态 |
| --- | --- |
| 统一 Provider 契约 | ✅ `lib/ai/vton/contract.ts` |
| Alibaba VTON Provider 接入 | ✅ 代码完成 + 服务端代理，⏸ 真实 Key 实测待补 |
| Provider 独立切换 | ✅ 实验台单选切换 |
| API Key 仅服务端 | ✅ 代理 `functions/api/vton/*`，前端零 Key |
| 20 组 Benchmark 可运行 | ✅ 实测 40/40（两个本地方案） |
| 所有 Provider 相同输入 | ✅ 固定数据集 `benchmark.ts` |
| 成功率/平均耗时/P50/P95/成本统计 | ✅ 实验台自动计算 |
| 人工质量评分 | ✅ 6 维 + 综合分 |
| JSON/MD 导出 | ✅ `VTON_BENCHMARK_YYYYMMDD.{json,md}` |
| 产品回归 | ✅ qa.mjs 17/17，实验室 qa-lab 13/13 |
| 代码提交并推送 | ✅ 见 Git 记录 |
| 生产产品未被云端 VTON 改动 | ✅ 默认关闭，仅实验台入口 |

**未完成事项（需用户提供 Key）：** 阿里云 aitryon 真实效果/延迟/成本实测。代码、代理、UI、统计、报告框架均已就绪，补齐 Key 后可直接运行。
