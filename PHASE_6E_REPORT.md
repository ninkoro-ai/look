# Phase 6E AI Try-On Value Validation

## 1. 实验目标

验证 AI 真实试穿是否产生用户价值：

1. 用户是否愿意主动使用 AI 试穿；
2. AI 试穿是否提升穿搭决策；
3. 用户是否愿意重复生成；
4. AI 试穿是否具备未来付费价值。

本阶段只做「用户价值验证」：不加功能、不改模型、不做商业化。**基础设施已全部完成并验证；真实用户数据待招募 10 名测试者 + 配置 Key 后采集。**

## 2. 测试用户

| 口径 | 数量 | 说明 |
| --- | --: | --- |
| 目标 | 10 | 女性、小红书用户、有购物/穿搭需求 |
| 当前真人 | 0 | 待招募 |
| 模拟验证 | 1 会话 | 自动化全流程（入口门禁→建衣橱→选衣→生成→失败兜底→事件→看板→成本导出） |

## 3. Alibaba AITryOn 配置

- Provider：`AlibabaAITryOnProvider`（`lib/ai/vton/providers/alibaba.ts`，VTON 抽象层与调用方式不变）
- Key：`DASHSCOPE_API_KEY` 仅服务端（Cloudflare Secret / `.dev.vars`），未进入前端、Git、日志
- 功能开关：`VTON_BETA_ENABLED=true`（服务端门禁）+ `NEXT_PUBLIC_VTON_BETA_ENABLED=true`（构建期前端开关）+ `VTON_ALLOW_ALIBABA=true`
- 双保险：前端入口仅 Beta 用户可见；服务端需 Key + Beta 开关 + Allow 开关三者同时满足才放行
- 当前线上状态：未配置 Key → 入口自动隐藏，普通用户与 Beta 用户均不会产生云端消耗

## 4. 调用统计

埋点事件（仅行为，无照片/身份）：

| 事件 | 说明 |
| --- | --- |
| `vton_started` | 开始试穿（provider=alibaba） |
| `vton_completed` | 生成完成（duration_ms / cost_estimate / success） |
| `vton_rated` | 评分 1~5（👍=5 / 👎=1） |
| `vton_saved` | 收藏 / 加入我的穿搭 |
| `vton_retry` | 再试一次 |
| `vton_pay_intent` | 付费意愿问卷 A/B/C |

模拟统计：1 次 started、0 次成功（无 Key 时的安全失败路径已验证）。

## 5. 成功率

| 状态 | 结果 |
| --- | --- |
| 模拟（无 Key） | 0%（优雅失败，显示「生成失败，请稍后重试」/「建议上传：正面全身照片」） |
| 真实 | 待 Key + 真人测试（官方：失败不收费） |

## 6. 平均耗时

官方口径 15–30 秒；等待页已实现三段式文案轮播（生成中/调整比例/优化真实效果），不暴露 Provider/API 术语；超时自动重试一次。

## 7. 成本统计

- 单价 ¥0.2/张（仅成功计费），免费额度 400 张/90 天
- 成本报告：`/lab/vton/beta` 实时统计 + 导出 `vton-cost-report.json`（逐次 provider/耗时/成本/成败）
- 10 用户月成本估算（假设每天 1 次、30 天全量真实试穿）：约 ¥60（$8.3）

## 8. 用户评分

- 已实现：结果页 1~5 星评分 + 👍 有帮助 / 👎 没帮助
- 统计：平均评分 + 评分次数（看板实时计算）
- 结果：待真人数据

## 9. 二次使用情况

- 已实现：`vton_retry` 手动再试事件 + 二次生成率统计（≥2 次 start 用户 / ≥1 次 start 用户）
- 结果：待真人数据

## 10. 收藏情况

- 已实现：❤️ 收藏、📂 加入我的穿搭（保存到本地 `vtonTests`，provider=beta-tryon）+ `vton_saved` 事件 + 收藏率统计
- 结果：待真人数据

## 11. 用户反馈

- 已实现：付费意愿问卷「如果每天生成次数有限，是否愿意购买 AI 试穿次数？A 愿意 / B 看价格 / C 不需要」（每次首个成功结果后展示一次）
- 结果：待真人数据

## 12. 最大问题

1. **无 `DASHSCOPE_API_KEY`**：AI 真实试穿无法真正出图，价值指标无法用真实数据回答；
2. **无真人测试者**：点击率/评分/二次生成/付费意愿全部待采集；
3. **无真实测试素材**：20 组真实图片（真人自拍/衣服照片/复杂场景）未放入 `public/bench-assets/alibaba/`；
4. **入口被正确隐藏**：未配置 Key 时换装间不显示「AI 真实试穿」，因此 Beta 用户目前看不到该功能（安全但功能未亮）；
5. **Mock/演示素材不代表真实效果**：真实质量分必须等真实图片。

## 13. 是否具备商业化价值

**暂不能下结论（数据未出），但验证体系已就绪。**

架构上符合预期：普通用户 Local 免费换装 + Beta 用户「AI 真实试穿」按次入口。下一步只需三步即可拿到答案：
1. 配置 `DASHSCOPE_API_KEY`（北京地域）→ 入口自动点亮；
2. 放入 20 组真实素材 → 跑通真实生成；
3. 招募 10 名目标用户跑 Day0 + Day1–7，回填本报告全部指标（点击率、成功率、平均评分、二次生成率、收藏率、付费意愿 A/B/C、10 用户月成本）。

届时即可明确 LOOK 的路线：AI 试穿助手（订阅）还是数字衣橱（管理+推荐）还是两者结合。

---

## 附：验收状态

| 验收项 | 状态 |
| --- | --- |
| Alibaba 真实 Key 接入 | ⏸ 代码/门禁就绪，Key 待配置（安全默认关闭） |
| Beta 用户 AI 入口 | ✅ 换装间「✨ AI真实试穿」入口（Beta + 开关 + 服务端就绪三条件） |
| AI 生成流程 | ✅ `/tryon` 全流程（选衣→等待→结果→失败兜底→自动重试） |
| Feedback 系统 | ✅ 6 类事件 + 评分 + 收藏 + 付费问卷 |
| 成本统计 | ✅ `/lab/vton/beta` + `vton-cost-report.json` 导出 + 10 用户月估算 |
| 用户行为统计 | ✅ 指标看板（次数/成功率/耗时/成本/评分/二次生成/收藏/付费意愿） |
| 实验页面 | ✅ `/lab/vton/beta` |
| 报告生成 | ✅ 本文件 |
| 回归测试 | ✅ qa 16/16 · qa-lab 13/13 · qa-wardrobe 11/11 · qa-import 13/13 · qa-model 10/10 · qa-beta 16/16 · qa-alibaba 8/8 · qa-tryon 9/9 |
| Git 提交 | ✅ `feat: phase 6e ai tryon value validation` |

**未完成事项（需真人/Key）：** 真实 AI 生成、10 人 Beta 测试、基于真实数据的用户价值分析。
