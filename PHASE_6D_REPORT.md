# Phase 6D Closed Beta Validation

## 1. 实验目标

Phase 6D 的唯一目标：通过 10～20 名真实测试用户，验证 LOOK 的核心假设——**用户是否愿意建立自己的数字衣橱并持续使用**。本阶段不做新功能、不接入商业化、不扩模型，只搭建并验证 Closed Beta 所需的全部基础设施：

- Beta 测试模式与数据隔离；
- 首次使用引导（≤3 步）与空衣橱入口；
- AI 识别失败兜底（重新上传 / 手动添加）；
- 行为埋点与核心指标统计；
- 轻量反馈入口；
- 隐私保护与数据可删除。

> 重要说明：本环境没有真人测试者。本阶段**全部基础设施已完成并验证**；核心指标使用 13 人模拟 Cohort（12 人注入历史事件 + 1 人自动化交互）验证统计口径。**真实用户数据待招募 Closed Beta 测试者后采集**，这是进入公开 Beta 的前置条件。

## 2. 测试用户数量

| 口径 | 数量 | 说明 |
| --- | --: | --- |
| 模拟 Cohort | 13 | 12 人注入历史事件（Day0–Day7）+ 1 人自动化真实流程 |
| 真实用户 | 0 | 待招募（10–20 人目标） |

## 3. 用户流程

```
测试入口：邀请链接 ?beta=1 或 /lab/beta 开启 Beta 模式（独立数据库）
  ↓
首页引导（≤3 步：为什么需要数字衣橱 → 3 步说明 → 立即开始）
  ↓
衣橱空态 CTA「添加我的第一件衣服」
  ↓
/import 上传（穿搭照片 / 单品照片）
  ↓
AI 识别 → 人工确认（改名/改类/删除）
  ↓
加入衣橱 → 每日推荐 / 换装间 / 反馈 / AI 试穿（lab）
```

## 4. 首次衣橱建立数据

定义：开始上传 → 至少成功加入 5 件衣物。

| 指标 | 结果（模拟） | 说明 |
| --- | ---: | --- |
| 完成率 | 69%（9/13） | 8/12 注入用户 + 1 交互用户完成 ≥5 件 |
| 平均首次完成时间 | 151s | 目标 <3 分钟，模拟达成 |
| 交互验证 | ✅ | 空衣橱 → 上传 → 识别 5 件 → 确认 → 入库 5 件全链路跑通 |

## 5. 上传行为分析

- 埋点：`wardrobe_onboarding_started`、`garment_upload_started`、`garment_detection_completed`、`garment_added`（含 category/source）；
- 模拟数据：8 名注入用户各上传 1 次穿搭照、检出 5 件、确认 5 件（100% 确认率，模拟）；
- 正式产品入口（空衣橱 CTA）与实验台共用同一套分析 schema，真实用户数据可直接复用统计口径；
- 单件处理耗时（Mock）毫秒级；真实 AI 预估 2–8s，仍低于 30s 目标。

## 6. AI 识别问题

- Mock 数据集构造一致，识别准确率 100%（流程验证口径）；
- **真实准确率未验证**：需 RealGarmentAIProvider + 真实用户照片，属 Beta 前置条件；
- 失败兜底已实现：AI 识别失败 → 「重新上传」或「手动添加一件」（名称 + 分类），流程不会中断；
- 已知风险：Mock 固定返回 5 件，无法度量真实误检/漏检对用户耐心的影响。

## 7. 每日推荐使用情况

| 指标 | 结果（模拟） |
| --- | ---: |
| 推荐查看用户占比 | 54%（7/13） |
| 推荐查看总次数 | 7 次 |
| 换装间访问 | 已埋点（`dress_page_viewed`），真人数据待采集 |
| 收藏数 | 已埋点（favorites store），真人数据待采集 |

## 8. AI 试穿点击情况

| 指标 | 结果（模拟） |
| --- | ---: |
| AI 试穿点击率 | 38%（5/13 用户） |
| 埋点 | `vton_clicked`（/lab/vton 运行触发，Beta 用户记录） |
| 说明 | 产品正式 VTON 入口按 Phase 6B 约定保持关闭，仅实验台可用 |

## 9. 用户反馈汇总

| 反馈 | 数量（模拟） |
| --- | --: |
| 😊 喜欢 | 3 |
| 😐 一般 | 1 |
| 😞 不喜欢 | 1 |
| 文字反馈 | 1 条（“推荐不够准”） |

入口：首页「今天推荐怎么样？」（仅 Beta 模式显示），反馈只记录情绪与可选文字。

## 10. 最大阻力

基于模拟与流程审计，已知阻力（真实优先级待真人数据确认）：

1. **搬衣成本**：用户必须自己拍照/上传并逐件确认，是第一道门槛；
2. **Mock 与真实识别差距**：真实误检会让确认成本上升，需真实 Provider 验证；
3. **AI 试穿不可用**：产品内没有正式 VTON 入口，“AI真实试穿”价值无法在产品内感知；
4. **每日推荐依赖衣橱数量**：衣物 <5 件时推荐价值有限，冷启动是关键；
5. **真人样本缺失**：目前所有“意愿/留存”数字均为模拟，不能代表真实用户。

## 11. 产品优化建议

- 招募 10–20 名真实测试者（女性、有真实衣橱），提供邀请链接 `?beta=1`；
- Beta 期接入真实识别（用户授权照片 + RealGarmentAIProvider 或人工兜底）；
- 降低确认成本：识别结果默认全选、只删错误项；失败路径已有兜底；
- 每日任务式引导：Day0 建 10 件衣橱、Day1–7 每天看 3 套推荐并完成一次搭配（任务书既定流程）；
- 用同一套 `betaEvents` schema 采集真人数据 1–2 周后，再评估留存与推荐价值；
- 产品内保留 Beta 退出入口（首页底部），用户可一键删除本地测试数据。

## 12. 是否进入公开 Beta

**结论：暂不建议进入公开 Beta。**

理由：
- ✅ Closed Beta 基础设施全部就绪并验证（模式隔离、引导、CTA、兜底、埋点、指标、反馈、隐私、删除）；
- ⚠️ 但真人数据为 0，“用户是否愿意持续使用”尚未被回答；
- ✅ 建议先按任务书执行 10–20 人 Closed Beta（Day0 建衣橱 + Day1–7 每日使用），1–2 周后依据真实留存/完成率/反馈决定是否公开 Beta。

---

## 附：验收状态

| 验收项 | 状态 |
| --- | --- |
| Beta 模式 | ✅ 独立 IndexedDB + 用户标识 + 可一键删除 |
| 首次使用流程 | ✅ 首页 ≤3 步引导（仅 Beta + 空衣橱显示） |
| 空衣橱 CTA | ✅ 衣橱页空态显示「添加我的第一件衣服」，不影响已有用户 |
| 真实用户测试入口 | ✅ 邀请链接 `?beta=1` + `/lab/beta` 开启 |
| 行为埋点 | ✅ 6 类任务书事件 + session/feedback/dress/vton 事件 |
| 数据统计 | ✅ `/lab/beta` 指标看板 + JSON 导出 |
| 用户反馈入口 | ✅ 首页三档情绪 + 可选文字（仅 Beta） |
| 隐私检查 | ✅ 事件无照片/姓名/手机号（QA 断言）；照片仅存本机 IndexedDB |
| PHASE_6D_REPORT.md | ✅ 本文件 |
| 回归测试 | ✅ qa 17/17 · qa-lab 13/13 · qa-wardrobe 11/11 · qa-import 10/10 · qa-model 10/10 · qa-beta 16/16 |
| Git 提交 | ✅ `feat: phase 6d closed beta validation` |

**未完成事项（需真人参与）：** 10–20 名真实用户招募、Day0–Day7 真实使用数据采集、基于真实数据的留存与意愿分析。
